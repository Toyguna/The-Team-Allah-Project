from util.settings import normalize_w, normalize_h, PLAYABLE_AREA

CURVE_PROJ_SIZE = normalize_w * 30, normalize_h * 30
BASIC_PROJ_SIZE = normalize_w * 30, normalize_h * 30
ALERT_PROJ_SIZE = normalize_w * 60, normalize_h * PLAYABLE_AREA[1]